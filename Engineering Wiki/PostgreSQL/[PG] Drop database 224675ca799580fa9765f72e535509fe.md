# [PG] Drop database

Owner: Nam Tran
Last edited time: December 3, 2025 11:30 AM

<aside>

Database name: `<database_name>`

Login user: `*<login_user>*`

Login password: `<login_user_password>`

Database user: `<db_user>`

</aside>

```sql
--Bring offline
update pg_database set datallowconn = false where datname = '<database_name>';
```

```sql
--Bring back online
update pg_database set datallowconn = true where datname = '<database_name>';
```

```sql
--Disconnect to database
SELECT 
    pg_terminate_backend(pid) 
FROM 
    pg_stat_activity 
WHERE 
    -- don't kill my own connection!
    pid <> pg_backend_pid()
    -- don't kill the connections to other databases
    AND datname = '<database_name>';
```

```sql
--Drop database
drop database '<database_name>';
```